# 🧪 Métodos de Objetos Preconstruidos en JavaScript

## 🎯 Objetivo de la Demo

Explorar y comprender los **métodos de los objetos preconstruidos `Math` y `String`** en JavaScript, aplicándolos en una pequeña aplicación por consola.

No se entregan soluciones directas: la idea es **investigar, probar y razonar** cada paso.

---

## 🧠 ¿Qué son los objetos preconstruidos?

JavaScript incluye objetos globales listos para usar, como `Math` y `String`, que facilitan operaciones matemáticas y manipulación de texto sin necesidad de crearlos manualmente.

---

## 🖥️ Paso 1: Acceder a los objetos en la consola

1. Abre el navegador.
2. Presiona **F12** o **Ctrl + Shift + I**.
3. Ve a la pestaña **Console**.
4. Escribe el nombre del objeto `Math` o `String` y observa qué aparece.

💡 _Pista:_ La consola permite explorar métodos escribiendo `Math.` y usando el autocompletado.

---

## 🔢 Paso 2: Métodos básicos del objeto Math

Explora métodos que modifican números:

- `abs()`
- `round()`
- `ceil()`
- `floor()`

💡 _Pista:_ Prueba con números **positivos y negativos** y observa las diferencias entre cada método.

---

## 🎲 Paso 3: Números aleatorios y cálculos

Investiga los siguientes métodos:

- `random()`
- `pow()`
- `sqrt()`

💡 _Pista:_

- `Math.random()` devuelve un número entre 0 y 1
- Piensa cómo podrías **llevar ese valor a un rango definido** (por ejemplo, entre 1 y 10).

---

## 🔎 Paso 4: Búsqueda de texto con String

Trabaja con una frase almacenada en una variable y explora:

- `indexOf()`
- `lastIndexOf()`
- `includes()`
- `startsWith()`
- `endsWith()`

💡 _Pista:_  
Prueba buscar palabras que:

- Existan
- No existan
- Estén al inicio o al final del texto

---

## ✏️ Paso 5: Modificación de texto

Usa métodos para transformar texto:

- `toUpperCase()`
- `toLowerCase()`
- `trim()`
- `replace()`
- `replaceAll()`

💡 _Pista:_  
Experimenta con textos que tengan **espacios al inicio y al final** o palabras repetidas.

---

## ✂️ Paso 6: Extraer y dividir texto

Explora cómo trabajar con partes de un texto usando:

- `slice()`
- `substring()`
- `split()`
- `concat()`

💡 _Pista:_

- `split()` es muy útil cuando un texto tiene separadores como comas o espacios.
- Compara qué ocurre cuando cambias los índices en `slice()` y `substring()`.

---

## 🧩 Paso 7: Ejercicio práctico integrador

Crea una pequeña aplicación en consola que:

1. Tome un texto definido por ti.
2. Limpie espacios innecesarios.
3. Busque una palabra específica.
4. Modifique parte del texto.
5. Realice un cálculo numérico usando `Math`.

💡 _Pista final:_  
Divide el problema en pasos pequeños y prueba cada método por separado antes de unir todo.

---

## 🧮 Ejercicio 8: Uso del Objeto Math

### 📌 Contexto 🙌

El objeto `Math` en JavaScript ofrece múltiples funciones matemáticas útiles, como cálculos de potencias, raíces y valores aleatorios.

### ✍️ Consigna

- Genera un número aleatorio entre **1 y 100**.
- Calcula la **raíz cuadrada** de un número dado.
- Redondea el número **5.7** al entero más cercano.

⏱️ **Tiempo estimado:** 20 minutos

### ⚙️ Paso a paso

1. Usa `Math.random()` para generar un número entre 1 y 100.
2. Usa `Math.sqrt()` para obtener la raíz cuadrada de un número.
3. Usa `Math.round()` para redondear el número 5.7.

💡 _Pista:_  
Piensa cómo combinar operaciones matemáticas para ajustar el rango del número aleatorio.

---

## 🔤 Ejercicio 9: Manipulación de Cadenas con el Objeto String

### 📌 Contexto 🙌

El objeto `String` permite trabajar con texto usando métodos como `toUpperCase()`, `toLowerCase()` y `replace()`.

### ✍️ Consigna

- Crea una variable con el texto **"JavaScript es genial"**.
- Convierte el texto a **mayúsculas**.
- Reemplaza la palabra **"genial"** por **"increíble"**.
- Cuenta cuántos caracteres tiene la cadena.

⏱️ **Tiempo estimado:** 20 minutos

💡 _Pista:_  
Recuerda que las cadenas tienen propiedades y métodos que puedes encadenar o ejecutar por separado.

---

## ✅ Cierre

Estos métodos forman parte del **uso diario de JavaScript**. Dominar `Math` y `String` te permitirá escribir código más claro, eficiente y expresivo.

¡Explora, equivócate y prueba! 🚀
